{
  "name": "plaintextpantry-client",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "dotnet fable watch -o fable-out --run vite",
    "build": "dotnet fable -o fable-out && vite build"
  },
  "dependencies": {
    "@journeyapps/wa-sqlite": "^2.0.4",
    "@powersync/web": "^2.3.1",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
